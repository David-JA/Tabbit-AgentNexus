interface:
  display_name: "AgentNexus Context Pack"
  short_description: "把当前 AgentNexus 仓库打成外部 AI 可读的 zip context pack"
  default_prompt: "Use $nexus-context-packager to package the current AgentNexus repo into an AI-readable context zip before external review or handoff."

policy:
  allow_implicit_invocation: true
